import * as esToolkit from "es-toolkit";
import * as fse from "fs-extra/esm";

//#region src/core/builder/replace.d.ts
declare function replace(contents: string, from: RegExp | RegExp[], to: string | string[]): string;
declare function replaceInFile({
  files,
  from,
  to,
  isGlob
}: {
  files: string | string[];
  from: RegExp | RegExp[];
  to: string | string[];
  isGlob?: boolean;
}): Promise<void>;
//#endregion
export { esToolkit, fse, replace, replaceInFile };